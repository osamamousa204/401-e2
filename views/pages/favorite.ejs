<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div id="allContainerfav">
        <%data.forEach(val=>{%>
        <div class="containerfav">
            <h3><%=val.name%></h3>
            <img src="<%=val.img_url%>" alt="digital">
            <p><%=val.level%></p>
            <a href="/details/<%=val.id%>"><button>Show details</button></a>
        </div>
        <%})%>
    </div>
</body>
</html>